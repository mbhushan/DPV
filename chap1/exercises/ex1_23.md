```py
Show that if a has a multiplicative inverse modulo N , then this inverse is unique (modulo N ).

Solution:

Lets prove contradiction:

Suppose x and y are distinct inverses of a mod N.
Then, it implies:
x = x * 1 = x*a*y = (x*a)*y = 1 * y = y (mod N) <= which is a contradiction
Q.E.D
```
